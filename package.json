{
  "name": "ghostline-zle",
  "description": "A ZSH widget that turns natural language into a single shell command.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "postinstall": "node ./scripts/patch-copilot-sdk.mjs",
    "install:ghostty": "bash ./scripts/install.sh --write-zshrc",
    "test": "node ./tests/copilot-helper.test.mjs && node ./tests/flight-log.test.mjs && node ./tests/project-context.test.mjs && node ./tests/logic-regressions.test.mjs && node ./tests/runtime-regressions.test.mjs && zsh ./tests/nl-detection.test.zsh && zsh ./tests/failure-indicator.test.zsh && zsh ./tests/message-queue.test.zsh && node ./tests/install-script.test.mjs",
    "check": "npm test && zsh -n ./copilot-zle.zsh && zsh -n ./ghostline-zle.zsh && zsh -n ./shell/copilot-zle-core.zsh"
  },
  "keywords": [
    "zsh",
    "ghostline",
    "terminal",
    "copilot",
    "cli",
    "command-generation"
  ],
  "dependencies": {
    "@github/copilot-sdk": "^0.2.0"
  }
}
